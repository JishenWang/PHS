<!-- 提示词：数据库字段名和代码中的字段映射必须完全一致，作为后续开发核心约束。 -->

# 字段一致性清单（后端联调基线）

## 核心约束

- 唯一字段来源：`c:\Users\15693\Desktop\pet_hospital.sql`
- 只允许在 SQL 中存在的列名出现在 Mapper SQL 里
- Java 侧允许使用驼峰字段，但必须通过 `as` 显式映射
- 任何新增接口/查询，先更新此清单再编码

---

## Mapper 结构约束（领域聚合）

- 采用“一个实体/领域模块一个 Mapper”，不采用“一张表一个 Mapper 文件”
- 推荐核心结构：
  - `UserMapper`
  - `DoctorMapper`
  - `PetMapper`
  - `AppointmentMapper`
  - `RegisterRecordMapper`
  - `OrderInfoMapper`
  - `PrescriptionMapper`
  - `MedicalRecordMapper`
  - `ConsultationMapper`
  - `CommonMapper`
- 旧兼容 Mapper（`OrderMapper`、`RegisterMapper`、`ReserveMapper`、`ServiceItemMapper`）已完成迁移并删除
- 实体目录同步收敛为领域实体（`User`、`Doctor`、`Pet`、`Reserve`、`RegisterRecord`、`OrderInfo`、`Prescription`、`MedicalRecord`、`Consultation`、`ServiceItem`）

---

## 最终目录快照（当前基线）

以下快照用于团队统一校验“是否和主分支一致”，以避免出现本地残留旧 Mapper 的情况。

### Java Mapper（`src/main/java/com/pethospital/pet_hospital/mapper`）

- `UserMapper.java`
- `DoctorMapper.java`
- `PetMapper.java`
- `AppointmentMapper.java`
- `RegisterRecordMapper.java`
- `OrderInfoMapper.java`
- `PrescriptionMapper.java`
- `MedicalRecordMapper.java`
- `ConsultationMapper.java`
- `CommonMapper.java`

### XML Mapper（`src/main/resources/mapper`）

- `UserMapper.xml`
- `DoctorMapper.xml`
- `PetMapper.xml`
- `AppointmentMapper.xml`
- `RegisterRecordMapper.xml`
- `OrderInfoMapper.xml`
- `PrescriptionMapper.xml`
- `MedicalRecordMapper.xml`
- `ConsultationMapper.xml`
- `CommonMapper.xml`

### 删除确认（不得恢复）

- `OrderMapper.java` / `OrderMapper.xml`
- `RegisterMapper.java` / `RegisterMapper.xml`
- `ReserveMapper.java` / `ReserveMapper.xml`
- `ServiceItemMapper.java` / `ServiceItemMapper.xml`

---

## 1) `sys_user`（系统用户）

- 对应实体：`com.pethospital.pet_hospital.entity.User`
- 对应 Mapper：
  - `src/main/resources/mapper/UserMapper.xml`
  - `src/main/java/com/pethospital/pet_hospital/mapper/UserMapper.java`

字段映射（当前已落地）：

- `id -> id`
- `username -> username`
- `password -> password`
- `real_name -> realName`
- `phone -> phone`
- `role -> role`
- `role_code -> roleCode`
- `primary_role_code -> primaryRoleCode`
- `email -> email`
- `status -> status`
- `created_time -> createdAt`
- `updated_time -> updatedAt`

---

## 2) `pet`（宠物档案）

- 对应实体：`com.pethospital.pet_hospital.entity.Pet`
- 对应 Mapper：
  - `src/main/resources/mapper/PetMapper.xml`
  - `src/main/java/com/pethospital/pet_hospital/mapper/PetMapper.java`

字段映射（当前已落地）：

- `id -> id`
- `owner_user_id -> ownerUserId`
- `owner_user_id -> ownerId`（兼容旧逻辑）
- `owner_name -> ownerName`
- `owner_phone -> ownerPhone`
- `name -> name`
- `species -> species`
- `gender -> gender`
- `age -> age`
- `allergy_history -> allergyHistory`
- `created_time -> createdAt`
- `updated_time -> updatedAt`

---

## 3) `doctor_profile`（医生档案）

- 对应实体：`com.pethospital.pet_hospital.entity.Doctor`
- 对应 Mapper：
  - `src/main/resources/mapper/DoctorMapper.xml`
  - `src/main/java/com/pethospital/pet_hospital/mapper/DoctorMapper.java`

字段映射（当前已落地）：

- `id -> id`
- `doctor_no -> doctorNo`
- `name -> name`
- `department -> dept`
- `dept_id -> deptId`
- `work_status -> workStatus`（通过 case 转为 `FREE/BUSY/REST`）
- `created_time -> createdAt`
- `updated_time -> updatedAt`

---

## 4) `appointment`（预约）

- 对应实体：`com.pethospital.pet_hospital.entity.Reserve`
- 对应 Mapper：
  - `src/main/resources/mapper/AppointmentMapper.xml`
  - `src/main/java/com/pethospital/pet_hospital/mapper/AppointmentMapper.java`

字段映射（当前已落地）：

- `id -> id`
- `appointment_no -> appointmentNo`
- `owner_user_id -> ownerUserId`
- `owner_id -> ownerId`
- `pet_id -> petId`
- `doctor_id -> doctorId`
- `service_item_id -> serviceItemId`
- `service_type -> serviceType`
- `appointment_time -> appointmentTime`
- `symptom_desc -> symptomDesc`
- `source_type -> sourceType`
- `status -> status`
- `status_text -> statusText`
- `created_time -> createdAt`
- `updated_time -> updatedAt`

---

## 5) `service_item`（服务项目）

- 对应实体：`com.pethospital.pet_hospital.entity.ServiceItem`
- 对应 Mapper：
  - `src/main/resources/mapper/CommonMapper.xml`
  - `src/main/java/com/pethospital/pet_hospital/mapper/CommonMapper.java`

字段映射（当前已落地）：

- `id -> id`
- `service_code -> serviceCode`
- `service_name -> serviceName`
- `category -> category`
- `dept_id -> deptId`
- `price -> price`
- `duration_minutes -> durationMinutes`
- `description -> description`
- `status -> status`
- `sort_no -> sortNo`

---

## 6) `prescription`（处方主表）

- 对应实体：`com.pethospital.pet_hospital.entity.Prescription`
- 对应 Mapper：
  - `src/main/resources/mapper/PrescriptionMapper.xml`
  - `src/main/java/com/pethospital/pet_hospital/mapper/PrescriptionMapper.java`

字段映射（当前已落地）：

- `id -> id`
- `prescription_no -> prescriptionNo`
- `register_id -> registerId`
- `record_id -> recordId`
- `pet_id -> petId`
- `doctor_id -> doctorId`
- `prescription_type -> prescriptionType`
- `diagnosis -> diagnosis`
- `total_amount -> totalAmount`
- `status -> status`
- `remark -> remark`
- `created_time -> createdAt`
- `updated_time -> updatedAt`

---

## 7) 已完成迁移说明

- `register_record`：已迁移到 `RegisterRecordMapper.xml` / `RegisterRecordMapper.java`
- `order_info`：已迁移到 `OrderInfoMapper.xml` / `OrderInfoMapper.java`

说明：前台列表场景仍以 `Map<String, Object>` 返回视图字段，但已统一到领域 Mapper 中，避免旧兼容 Mapper 分叉维护。

---

## 开发流程（强制）

1. 先在 `pet_hospital.sql` 中确认表和列
2. 在本清单登记映射关系
3. 再写 `Entity`、`Mapper.java`、`Mapper.xml`
4. 执行 `mvn -DskipTests compile` 验证

