<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pethospital.pet_hospital.mapper.PetMapper">

    <select id="listByOwnerId" resultType="com.pethospital.pet_hospital.entity.Pet">
        select id,
               owner_user_id as ownerId,
               name,
               species,
               gender,
               age,
               allergy_history as allergyHistory,
               '' as healthRecords,
               created_time as createdAt,
               updated_time as updatedAt
        from pet
        where is_deleted = 0
          and (owner_user_id = #{ownerId} or owner_id = #{ownerId})
        order by id desc 
    </select>

</mapper>
